# Taipei Family Radar
#
# 網站本身（runtime / build）不需要任何環境變數。
# 以下僅在「方式 B：GitHub Actions cron 備援自動化」時需要，設定於
# GitHub → Settings → Secrets，而非本機 .env。
#
# ANTHROPIC_API_KEY=   # 每週掃描代理人使用
# PAT_TOKEN=           # 讓機器人 push 能觸發 Vercel 部署（repo 權限的 GitHub PAT）
